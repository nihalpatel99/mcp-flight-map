[project]
name = "flight"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Nihal Patel", email = "nihal1999patel@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "httpz>=1.0",
    "mcp[cli]>=2.0.0",
]

[project.scripts]
flight = "flight:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
